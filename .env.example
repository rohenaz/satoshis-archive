# Application URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# NOTE: Anna's Archive API keys are NOT stored on the server!
# Users provide their own API keys which are stored in their browser's localStorage.
# This ensures the platform owner has no knowledge of user activities.
